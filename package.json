{
  "name": "oussama-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve . -l ${PORT:-3000}",
    "start": "serve . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
